package main

import (
	"testing"
)

func TestWebhook(t *testing.T) {
	// output := health.DiskUsage()
	// if strings.Contains(output, "Filesystem") {
	// 	t.Errorf("Expected output to contain 'Filesystem', got %s", output)
	// }

	// size := 30
	// exts := []string{"jpg", "png"}
	// output = health.BigFiles(size, exts)
	// if strings.Contains(output, "Filesystem") {
	// 	t.Errorf("Expected output to contain 'Filesystem', got %s", output)
	// }

	// fmt.Println(output)
}
